from RoguelikeMapModuleBase import *

class DungeonModeModule(TileMapModuleBase):
    def __init__(self, mapSize, tileSize, window, textures):
        TileMapModuleBase.__init__(self, mapSize, tileSize, window, textures)

    
